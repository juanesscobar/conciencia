# CONCIENCIA — FINAL ARCHITECTURE REVIEW, HARDENING & PRODUCTION READINESS

Context:
You are working on Conciencia Platform, an AI Mission Control Plane / Agent Orchestrator.

The complete implementation roadmap A–L has already been finished.

Current baseline:
- Branch: v2-refactor
- Full suite: 291 passed
- 8 deselected tests correspond to pre-existing real-provider LLM Harness tests
- A–L completed
- Real E2E verified locally
- Do NOT redesign the product from scratch
- Do NOT introduce speculative features
- Do NOT create a new roadmap yet
- Preserve all existing behavior unless there is a demonstrated defect, architectural inconsistency, security issue, or production-readiness blocker

Important:
This is NOT another implementation phase.

This is the final architecture audit, hardening pass, integration review and production-readiness checkpoint before daily usage and deployment.

The primary objective is:

VERIFY THAT CONCIENCIA CAN SAFELY, CONSISTENTLY AND AUDITABLY RECEIVE A MISSION, SELECT THE CORRECT RESOURCES, EXECUTE IT, PRESERVE EVIDENCE, CONTROL COST/RISK AND EXPLAIN EXACTLY WHAT HAPPENED.

==================================================
1. FIRST: UNDERSTAND THE SYSTEM BEFORE MODIFYING IT
==================================================

Inspect the repository thoroughly.

Read:
- README
- architecture docs
- migrations/models
- API routers
- CLI
- services
- workflow engine
- mission engine
- registries
- tests
- Docker/deployment files
- env configuration
- current git diff/status
- recent commits F–L

Reconstruct the actual architecture from code.

Do not assume that documentation reflects reality.

Produce an internal architecture map containing at least:

Mission
→ Workflow
→ Team
→ Agent
→ Harness
→ Context Pack
→ Tools / WebMCP
→ Execution
→ Observability
→ Signals
→ Evidence
→ Approval
→ Economics

Identify where these objects are:
- persisted
- resolved
- mutated
- executed
- observed
- exposed through API
- exposed through CLI

Before changing code, compare intended contracts against actual implementation.

==================================================
2. PRESERVE THE BASELINE
==================================================

The current regression baseline is:

291 passed
8 deselected

Run the complete existing suite before modifying anything.

Record:
- passed
- failed
- skipped
- deselected
- duration

Do NOT delete, weaken, skip or rewrite tests merely to obtain green output.

Do NOT change semantics silently.

Every fix introduced during this review must:
- preserve current passing behavior
- receive a regression test when appropriate
- keep the final suite >= current baseline

If a historical test is incorrect, document exactly why before modifying it.

==================================================
3. END-TO-END ARCHITECTURE CONTRACT REVIEW
==================================================

Audit the complete path:

User / CLI / API
        ↓
Mission
        ↓
Workflow
        ↓
Agent / Team resolution
        ↓
Harness resolution
        ↓
Context Pack retrieval
        ↓
Tool access
        ↓
Execution
        ↓
Observability
        ↓
Signals + Evidence
        ↓
Approval / Result
        ↓
Economics

Look specifically for:
- duplicated resolution logic
- inconsistent fallback order
- decorative fields that are persisted but ignored
- optional fields accidentally behaving as required
- required fields accidentally treated as optional
- differences between API and CLI behavior
- execution paths bypassing policies
- hidden global state
- services using SessionLocal directly instead of dependency/session injection
- transaction boundary problems
- partially persisted runs
- race conditions
- inconsistent IDs
- orphaned records
- inconsistent enums/status names
- dead code
- legacy paths from previous architecture versions

Do not refactor merely for aesthetics.

Refactor only where it materially improves correctness, safety, maintainability or observability.

==================================================
4. MISSION RESOLUTION SEMANTICS
==================================================

Verify and document the exact resolution priority.

Expected current conceptual behavior:

Explicit step.agent_id
→ Team agents
→ mission.agent_ids pool
→ global registry

Check that this applies consistently across:
- sequential steps
- parallel children
- retries
- resumed runs
- approval continuation
- CLI-created missions
- API-created missions

Review soft vs hard requirements:

capabilities
= preference / soft requirement

required_capabilities
= hard constraint

Ensure this distinction is consistently honored.

A missing hard requirement MUST fail clearly.

A missing soft capability MAY fall back to declarative/non-agent execution only where intended.

No silent incorrect agent dispatch.

==================================================
5. TEAM ORCHESTRATION
==================================================

Verify:
- team membership resolution
- team capability matching
- deterministic tie handling
- missing/deleted agents
- archived/inactive agents
- max_parallel
- fan-out/fan-in behavior
- child failure behavior
- partial outputs
- aggregated tokens
- aggregated costs
- aggregated duration
- session isolation
- database/session safety under concurrency

Parallel execution must not corrupt shared state.

One child failure must produce the documented behavior and must never silently disappear.

==================================================
6. HARNESS CONTRACT
==================================================

Verify Harness remains a reusable execution contract and does NOT become a second Agent model.

Audit:

instructions
context
tools
validation
guardrails
runtime.allowed
output_contract
version/history

Confirm:
- inactive harnesses cannot execute unless explicitly intended
- mission harness resolution works
- step harness override works
- harness versions remain traceable
- previous versions are preserved
- runtime allow-list is enforced before dispatch
- tool policy cannot be bypassed
- post-dispatch output validation always occurs where required
- validation failures are observable and explainable

Check placeholder behavior:

{objective}
{project_name}
{context_pack}

Missing placeholders or missing values must fail predictably or render predictably.

Never silently inject Python representations or malformed data into prompts.

==================================================
7. CONTEXT PACKS
==================================================

Audit context retrieval carefully.

Current behavior:
- keyword scoring
- title weight 3x
- content keys 2x
- values 1x
- packs with no match excluded
- top-K
- max_chars
- truncation tracking
- explicit mission context_pack_id overrides retrieval
- otherwise top relevant packs are selected

Verify:
- deterministic ranking
- case normalization
- punctuation handling
- duplicate terms
- empty objectives
- empty packs
- very large packs
- malformed content
- max_chars edge cases
- top_k edge cases
- project scoping
- tenant/workspace isolation if applicable

Most importantly:

NO unrelated project information should leak into another mission.

Context retrieval should remain cheap and deterministic unless the architecture explicitly requests otherwise.

Do not introduce vector databases, embeddings or semantic search during this pass unless they already exist and are malfunctioning.

==================================================
8. OBSERVABILITY
==================================================

Audit workflow_runs.events and step_results.

A complete run should make it possible to reconstruct:

- workflow start
- each step start
- each dispatch
- agent
- harness
- runtime
- provider
- model
- tool calls
- tokens
- cost
- duration
- parallel block lifecycle
- approval events
- errors
- final status

Check for impossible state combinations such as:
- completed run with failed required step
- failed step with no failure event
- approval-required step appearing completed
- child step cost missing from parent totals
- duplicated token accounting
- negative duration
- duplicate lifecycle events

Ensure CLI:

conciencia run inspect <id> --steps
conciencia run watch <id>
--json

reflect the same canonical data.

Human-readable logs should derive from structured data, not become a competing source of truth.

==================================================
9. SIGNALS + EVIDENCE
==================================================

Audit provenance.

Expected chain:

Signal
→ Evidence
→ Step
→ Run
→ Mission
→ Agent / Harness / Tool call

Verify:
- evidence IDs are valid
- mission.evidence_ids remains consistent
- extraction from parallel children works
- duplicate extraction behavior
- manual signal creation
- automatic extraction
- deleting/archive behavior
- malformed markers
- multiline outputs
- repeated SIGNAL/EVIDENCE markers

Do not treat LLM narration as verified truth.

A Signal is a finding, hypothesis, risk, opportunity, lead, decision or insight.

Evidence is the support attached to it.

Preserve that distinction.

Where evidence came from a tool call, retain enough provenance to identify:
- source
- tool
- timestamp
- run
- step

Do not fabricate evidence.

==================================================
10. WEBMCP REVIEW
==================================================

WebMCP must remain a Tool/Adapter, not become a parallel orchestration subsystem.

Audit the complete flow:

Mission
→ Agent/Harness
→ Tool permission
→ WebMCP adapter
→ WebMCP-enabled application
→ result
→ tool_call metadata
→ Evidence
→ Observability

Verify:
- capability/tool discovery
- schemas
- argument validation
- unknown tool behavior
- unreachable application behavior
- timeout behavior
- invalid response behavior
- read vs write actions
- side effects
- evidence preservation
- structured errors

Do NOT rely on brittle UI selectors where structured WebMCP tools exist.

WebMCP should expose structured capabilities to agents.

==================================================
11. TOOL SECURITY AND SIDE EFFECTS
==================================================

Audit every tool execution path.

Classify operations conceptually as:

READ
WRITE
DESTRUCTIVE
EXTERNAL_SIDE_EFFECT

Examples of external side effects:
- creating records
- modifying remote systems
- submitting forms
- sending messages
- changing prices
- deleting data
- publishing content
- financial/transactional operations

Ensure approval gates and guardrails cannot be bypassed through:
- parallel steps
- retries
- nested execution
- WebMCP
- direct runtime calls
- CLI
- API

Never log secrets.

Review:
- API keys
- bearer tokens
- provider credentials
- database URLs
- environment variables
- WebMCP credentials

Redact sensitive values from:
- events
- logs
- exceptions
- tool_calls
- CLI output
- API responses

==================================================
12. APPROVALS
==================================================

Audit the approval state machine.

Verify:
- approval_required
- approved
- rejected
- resumed
- repeated approval attempts
- invalid approval IDs
- approving finished runs
- rejecting finished runs
- concurrent approval calls
- side effects do not occur before required approval

Approval must be an execution control, not just a visual state.

==================================================
13. ECONOMICS
==================================================

Review the completed Economics layer.

Verify:
- prompt tokens
- completion tokens
- total tokens
- per-step cost
- parallel child cost
- mission total
- missing provider pricing
- unknown models
- simulated executions
- retries
- failed calls
- zero-cost providers
- rounding behavior

Ensure totals cannot silently undercount.

If cost data is unknown, expose it as unknown/unavailable rather than assuming zero unless zero is semantically correct.

Check any existing:
- cost limits
- budgets
- runtime economics
- model selection logic

Do not invent a new billing system.

==================================================
14. FAILURE MODEL
==================================================

Create a clear failure taxonomy using existing architecture where possible.

Review at least:

validation failure
agent resolution failure
runtime unavailable
provider unavailable
tool failure
WebMCP failure
timeout
approval rejection
harness guardrail failure
output contract failure
context failure
parallel child failure
database failure

Errors should:
- preserve machine-readable type/code where possible
- include useful human-readable message
- reference run/step
- avoid secrets
- appear in observability

Do not collapse everything into generic "execution failed".

==================================================
15. RETRIES AND IDEMPOTENCY
==================================================

Audit retry behavior.

A retry must not accidentally repeat destructive side effects.

Look for:
- duplicate tool calls
- duplicate WebMCP writes
- duplicate signals
- duplicate evidence
- duplicate external requests

Where destructive or external-side-effect operations already exist, verify the current architecture has sufficient protection.

If there is a concrete production blocker, implement the smallest safe correction.

Do not design a massive distributed idempotency platform.

==================================================
16. API / CLI CONSISTENCY
==================================================

Review every major domain:

missions
workflows
agents
teams
harnesses
context packs
runs
signals
evidence
WebMCP
economics

Check that CLI and API:
- use the same services
- enforce the same validation
- enforce the same policies
- return consistent states
- do not contain duplicated business logic

The CLI should be a client/interface to domain logic, not an independent implementation.

==================================================
17. STORAGE / DATABASE REVIEW
==================================================

Review:
- relationships
- foreign keys where applicable
- cascade behavior
- indexes
- uniqueness
- nullable fields
- JSON fields
- schema defaults
- migrations
- transaction boundaries
- concurrency-sensitive writes

Check for potential orphan records among:

missions
runs
step results
events
signals
evidence
teams
agents
harness versions
context packs

Do not redesign the persistence layer unless required.

==================================================
18. CONCURRENCY REVIEW
==================================================

Because parallel mission execution now exists, inspect thread/concurrency safety.

Pay particular attention to:
- SQLAlchemy sessions
- shared mutable objects
- workflow state
- events
- aggregate counters
- tokens/cost accumulation
- error propagation
- logging
- child result ordering

Every parallel worker should use appropriate session isolation.

Do not allow one thread to reuse an unsafe DB session owned by another execution context.

==================================================
19. PRODUCTION CONFIGURATION
==================================================

Audit deployment configuration.

Review:
- Dockerfiles
- docker-compose
- env vars
- ports
- volumes
- health checks
- restart policies
- database persistence
- migration execution
- logging
- production/debug modes
- CORS
- host binding
- secrets
- default credentials
- provider configuration

No development-only defaults should accidentally become production defaults.

Do not modify infrastructure unrelated to Conciencia.

==================================================
20. SECURITY PASS
==================================================

Perform a practical security review.

Look for:
- unauthenticated privileged routes
- authorization bypass
- overly broad CORS
- injection opportunities
- path traversal
- arbitrary command execution
- unsafe subprocess calls
- unvalidated URLs
- SSRF risk
- WebMCP origin trust
- secrets in logs
- insecure deserialization
- dangerous YAML loading
- SQL injection
- cross-project data exposure

Do NOT perform destructive security testing against external systems.

Use code inspection and local tests.

==================================================
21. GENTLE-AI / OPENCLAW INSPIRATION REVIEW
==================================================

Do NOT copy these products.

Do NOT add speculative features.

Only compare existing Conciencia architecture conceptually against useful ideas we have previously identified:

Gentle-AI:
- persistent memory / Engram concept
- Skill Registry
- organic routing
- Receipt-Driven Development
- Spec-Driven Development for BUILD missions

OpenClaw / modern agent environments:
- multiple agent runtimes
- sessions
- project isolation
- runtime adapters
- CLI-oriented execution

The goal of this section is only to identify future opportunities or architectural compatibility.

Write them into a FUTURE_NOTES document.

Do not implement them unless a missing piece is an immediate blocker for production correctness.

==================================================
22. MISSION RECEIPT READINESS
==================================================

Without implementing a new subsystem unless already almost available, verify whether existing data could reconstruct a future Mission Receipt:

mission_id
run_id
objective
workflow
agents
team
harness/version
context packs
tools
tool calls
signals
evidence
approvals
tokens
cost
duration
status
errors

Document any missing provenance field.

Do not create an unnecessary duplicate storage layer.

Prefer deriving receipts from canonical existing records.

==================================================
23. DAILY USE / DOGFOODING READINESS
==================================================

Verify these representative usage paths:

A. Research mission
B. Lead research mission
C. Software engineering mission
D. Data/audit mission
E. Parallel team mission
F. Mission with explicit harness
G. Mission with automatic Context Pack retrieval
H. Mission producing Signals/Evidence
I. Mission requiring approval
J. Mission using WebMCP
K. Failed mission
L. Cost/token inspection

Use mocks/local adapters where external access is unavailable.

Where real provider configuration is already present locally and safe, existing E2E procedures may be reused.

Do not incur unnecessary external cost.

==================================================
24. DOCUMENTATION
==================================================

Update documentation to reflect ACTUAL architecture after review.

At minimum produce/update:

ARCHITECTURE.md
PRODUCTION_READINESS.md
OPERATIONS.md
SECURITY.md
CHANGELOG.md

If equivalent documents already exist, update them rather than duplicating them.

ARCHITECTURE.md should include the canonical flow:

Mission
→ Workflow
→ Team / Agent
→ Harness
→ Context
→ Tools
→ Execution
→ Observability
→ Signals/Evidence
→ Approval
→ Economics

PRODUCTION_READINESS.md should clearly separate:

READY
READY WITH LIMITATIONS
EXPERIMENTAL
NOT IMPLEMENTED

Do not market unfinished capabilities as complete.

==================================================
FRONTEND / CONTROL PLANE UI INTEGRATION REVIEW
==================================================

A Conciencia frontend already exists.

DO NOT redesign it from scratch.
DO NOT replace its visual identity.
DO NOT create a second frontend.

Treat the existing frontend as the future operational
Control Plane UI for the A–L architecture.

First inspect the existing frontend implementation and determine:
- framework
- routes
- API clients
- state management
- mocked data
- real backend integrations
- obsolete components
- current navigation
- responsive behavior

Preserve its established visual identity:
- dark control-center aesthetic
- Conciencia branding
- HQ visualization
- terminal-inspired interface
- operational dashboard concept

The objective is NOT a visual redesign.

The objective is to determine whether the existing UI can faithfully
represent the current backend architecture.

Map existing UI concepts against:

Mission
Run
Workflow
Team
Agent
Harness
Context Pack
Tool
WebMCP
Signal
Evidence
Approval
Economics

Identify:
- screens backed by mock/static data
- screens already using real APIs
- missing API integration
- obsolete concepts
- duplicated frontend state
- backend capabilities with no UI representation

Prioritize DAILY OPERATION visibility.

A user should ultimately be able to understand from the UI:

WHAT is running
WHY it is running
WHO/WHAT agent is executing
WHICH team is involved
WHICH harness is active
WHAT context was supplied
WHICH tools were called
WHAT it cost
WHAT signals were found
WHAT evidence supports them
WHETHER human approval is required
WHY something failed

Review the existing HQ agent visualization.

Determine how it can represent real runtime state rather than
decorative/static agents.

Prefer mapping HQ entities to actual:
- agents
- teams
- active runs
- execution states

Review the Activity Log.

Prefer deriving it from workflow_runs.events rather than maintaining
a separate frontend activity source.

Review Metrics.

Prefer canonical observability/economics data:
- active missions
- completed runs
- failure rate
- tokens
- cost
- duration
- active agents
- pending approvals
- signals

Do not implement speculative analytics.

Review navigation and information architecture but make only
minimal changes required for the A–L architecture.

Do not remove domain modules such as Leads merely because they are
not core orchestration primitives.

Domain modules may live ABOVE the Conciencia control plane.

Example:

LeadHunter
      ↓
Mission
      ↓
Conciencia orchestration

The frontend should progressively become a real operational window
into Conciencia rather than a parallel application with independent
business logic.

Frontend business logic must not duplicate backend orchestration logic.

At the end classify frontend readiness:

CONNECTED
PARTIALLY_CONNECTED
PROTOTYPE_ONLY

Document exactly what is still mocked or disconnected.

Do NOT perform a complete frontend redesign during this audit.

==================================================
25. FINAL TESTING
==================================================

After all justified fixes:

Run targeted tests for changed components.

Then run the entire suite.

Expected minimum baseline:

291 passed
8 deselected

Report exact final numbers.

Also run:
- lint/type checks if configured
- migration checks if configured
- CLI smoke tests
- API startup smoke test
- docker/config validation if available

Do not introduce a new lint/type tool during this task unless already configured.

==================================================
26. FINAL OUTPUT
==================================================

At completion, provide a concise but auditable final report with:

A. Baseline
B. Findings
C. Bugs discovered
D. Fixes implemented
E. Security findings
F. Architecture inconsistencies
G. Production blockers remaining
H. Known limitations
I. Test results
J. Files changed
K. Deployment recommendation

Classify the final result as exactly one of:

READY_FOR_RC_DEPLOY
READY_WITH_KNOWN_LIMITATIONS
NOT_READY_FOR_PRODUCTION

Do not call the system production-ready merely because tests pass.

==================================================
27. GIT SAFETY
==================================================

Work only on the current v2-refactor branch.

Before modification:
- git status
- preserve unrelated user changes

Do not:
- reset --hard
- force push
- rewrite history
- delete user work
- modify unrelated projects

Commit the final review only after tests pass.

Suggested commit:

chore: production readiness audit and hardening

Do not push unless the environment/user workflow already explicitly permits pushing.

==================================================
28. CORE PRODUCT PRINCIPLES — DO NOT BREAK
==================================================

Conciencia is NOT another chatbot.

Conciencia is NOT a replacement for Codex, Claude Code, OpenClaw or specialized agents.

Conciencia is a control plane capable of orchestrating them.

Maintain these separations:

Agent
= who can perform work

Harness
= how work should be performed

Context Pack
= what information is relevant now

Tool
= what external capability can be used

Workflow
= how work is sequenced

Team
= which agents collaborate

Mission
= why the work exists / desired outcome

Observability
= what happened

Signal
= what was found

Evidence
= what supports the finding

Approval
= human governance

Economics
= resource/cost accountability

Do not collapse these concepts.

==================================================
29. NON-GOALS
==================================================

During this task DO NOT:

- add another agent framework
- add embeddings/vector DB just because they are fashionable
- add autonomous self-modification
- add complex long-term memory
- create new UI redesigns
- replace current database
- replace current workflow engine
- rewrite the project
- build an entire policy engine
- create dozens of new abstractions
- add speculative enterprise features
- optimize prematurely
- introduce breaking API changes without necessity

This task is about consolidation.

==================================================
30. DEFINITION OF DONE
==================================================

This final review is DONE when:

1. Existing architecture has been inspected end-to-end.
2. Concrete production-readiness defects found have been corrected.
3. No unnecessary architecture has been introduced.
4. Security-sensitive paths have been reviewed.
5. WebMCP is confirmed as a governed tool/adapter.
6. Parallel execution is session-safe.
7. Harness/tool/context policies cannot be trivially bypassed.
8. Signals/Evidence preserve provenance.
9. Observability can reconstruct execution.
10. Economics totals are trustworthy.
11. API/CLI behavior is consistent.
12. Deployment configuration has been reviewed.
13. Documentation reflects reality.
14. Full regression suite remains at least:

291 passed
8 deselected

15. A final readiness classification is produced.

At the end STOP.

Do not automatically start the next roadmap.
Do not invent Phase M.
Do not deploy automatically.

Return the final audit report and wait for human approval.